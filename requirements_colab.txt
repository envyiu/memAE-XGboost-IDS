# Requirements for Google Colab (Python 3.10/3.11, CUDA 12.x)
# Install with: pip install -r requirements_colab.txt
#
# Note: torch is pre-installed on Colab with CUDA support.
# Run the cell below first to check your Colab runtime:
#   import torch; print(torch.__version__, torch.cuda.is_available())
# If torch is already >=2.0 with CUDA, skip the torch line below.

pandas>=2.0,<3.1
numpy>=1.24,<3.0
pyarrow>=12.0
scikit-learn>=1.3,<2.0
torch>=2.0                  # pre-installed on Colab; upgrade only if needed
xgboost>=2.0,<4.0
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0
joblib>=1.3
tqdm>=4.65
scipy>=1.10                 # required by scikit-learn permutation_importance
